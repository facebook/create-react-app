import React from 'react';

export default class SampleComponent extends React.Component {
    render () {
        return (
            <div>
                <p>This is the <code>SampleComponent.js</code> component. It is displayed on the <code>/samplecomponent</code> route.</p>
            </div>
        )
    }
}
